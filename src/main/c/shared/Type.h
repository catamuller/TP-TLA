#ifndef TYPE_HEADER
#define TYPE_HEADER

typedef enum {
	false = 0,
	true = 1
} boolean;

typedef int Token;
typedef int Integer;
typedef char* Note;
typedef char* Chord;
typedef char* Rest;
typedef char* Instrument;

#endif
